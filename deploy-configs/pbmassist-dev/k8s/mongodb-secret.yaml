apiVersion: v1
kind: Secret
metadata:
  name: mongodb-credentials
  namespace: pbmassist-dev
  labels:
    app: pss-myclaims-ai-agent
    component: mongodb
type: Opaque
stringData:
  # UAT Connection String
  # Format: mongodb+srv://myClaims_uat:<password>@mdb-use4-myclaims-dev01-pl-0.knpouh.mongodb.net/?retryWrites=true&w=majority
  connection_string: "mongodb+srv://myclaims_dev:1sw2tSZ89tjpn1sm@mdb-use4-myclaims-dev01-pl-0.knpouh.mongodb.net/?retryWrites=true&w=majority"
  
  # Alternative: Store password separately if needed
  # password: "cw5uPntYMO3HMrso"
  # username: "myClaims_uat"
  # host: "mdb-use4-myclaims-dev01-pl-0.knpouh.mongodb.net"

---
# Note: To use this secret in your deployment, add to envVars:
# - name: MONGODB_CONNECTION_STRING
#   valueFrom:
#     secretKeyRef:
#       name: mongodb-credentials
#       key: connection_string

